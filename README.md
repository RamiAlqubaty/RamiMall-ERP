### Rami Mall

Mall have everythings

### Installation

You can install this app using the [bench](https://github.com/frappe/bench) CLI:

```bash
cd $PATH_TO_YOUR_BENCH
bench get-app $URL_OF_THIS_REPO --branch develop
bench install-app rami_mall
```

### Contributing

This app uses `pre-commit` for code formatting and linting. Please [install pre-commit](https://pre-commit.com/#installation) and enable it for this repository:

```bash
cd apps/rami_mall
pre-commit install
```

Pre-commit is configured to use the following tools for checking and formatting your code:

- ruff
- eslint
- prettier
- pyupgrade

### CI

This app can use GitHub Actions for CI. The following workflows are configured:

- CI: Installs this app and runs unit tests on every push to `develop` branch.
- Linters: Runs [Frappe Semgrep Rules](https://github.com/frappe/semgrep-rules) and [pip-audit](https://pypi.org/project/pip-audit/) on every pull request.



 


NOVEMBER 27 – 2025
 


Eng.Rami Al-Qubaty

 
# نظام إدارة المخزون والموارد البشرية والمبيعات للمولات التجارية

> **التاريخ:** 27 نوفمبر 2025  
> **المطور:** Eng. Rami Al-Qubaty  

نظام متكامل لإدارة **المخزون، الموارد البشرية، المبيعات، المشتريات** للمولات التجارية أو الأعمال المشابهة.  
يسهّل النظام عمليات البيع والشراء، متابعة المخزون، إدارة العملاء والموردين والموظفين، وإصدار التقارير المالية والإدارية.

---

## 🎯 أهداف النظام

- تنظيم وإدارة عمليات **البيع والشراء**.
- إدارة **المخزون** وتتبع الكميات المتبقية لكل منتج في كل مخزن.
- إدارة **الموارد البشرية** (موظفين، عملاء، موردين) مع صلاحيات مختلفة.
- إدارة **الحسابات المالية** للعملاء والموردين (مبالغ متبقية، دفعات، أقساط).
- توفير **تقارير تفصيلية** تساعد في اتخاذ القرارات الإدارية.

---

## 👥 أولاً: شرح الموارد البشرية

### 1. العملاء
- إضافة عملاء بسهولة عبر مستند العملاء.
- تخزين بيانات العميل لتسهيل عمليات البيع وترتيبها لكل عميل.
- إنشاء فواتير بيع باسم العميل والرجوع لسجل تعاملاته.

### 2. الموظفون / مستخدمو النظام
- إضافة كل مستخدم وتحديد الوظيفة الخاصة به.
- منح مجموعة صلاحيات حسب وظيفة المستخدم.
- منع تداخل الصلاحيات بين الموظفين.
- **فقط المشرف العام / مدير النظام** يمكنه منح الصلاحيات للمستخدمين الجدد.

### 3. الموردون
- إضافة كل مورد وتخزين بياناته.
- حقل خاص بالمبلغ المتبقي لكل مورد يتم تحديثه تلقائياً بعد كل عملية مالية مرتبطة به.
- ترتيب المعاملات المالية مع الموردين وربطها بالفواتير وسندات الدفع.

> **ملاحظة:**  
> يوجد صلاحيات جاهزة لكل وظيفة، وكذلك الموديلات (الشاشات) التي ستظهر لكل موظف حسب وظيفته.

---

## 📦 ثانياً: إدارة المخازن والمخزون

- عند إضافة بضاعة إلى المخزن يتم **تحديث المخزون تلقائياً**.
- تحديد الكميات المضافة لكل منتج بحسب:
  - المنتجات
  - الفئات
  - المخازن
- إمكانية:
  - **إدخال بضاعة**
  - **إخراج بضاعة**
  - **نقل بضاعة بين المخازن**
- معرفة الكمية المتبقية من كل منتج في كل مخزن.
- يتم إنشاء مستند خاص لكل منتج في مستند المخزون عند أول عملية إدخال بضاعة.

> **قيد مهم:**  
> يوجد مستند واحد لكل منتج في كل مخزن.  
> يمكن أن يوجد أكثر من مستند لنفس المنتج لكن في مخازن مختلفة.  
> النظام يمنع تكرار نفس المنتج في نفس المخزن.

---

## 💰 ثالثاً: عمليات البيع والشراء

### 1. عمليات البيع

- عند إنشاء **طلبية بيع**:
  - اختيار العميل.
  - اختيار المنتجات والكميات لكل منتج.
  - جلب السعر تلقائياً من مستند المنتج.
  - حساب إجمالي الفاتورة تلقائياً (سعر × كمية لكل منتج، ثم إجمالي نهائي).
  - تحديد المخزن الذي سيتم الخصم منه.

- بعد **تأكيد الفاتورة**:
  - يتم خصم الكميات من المخزن المحدد لكل منتج.
  - يمنع النظام البيع إذا كانت الكمية المطلوبة أكبر من المخزون المتبقي.

- عند إنشاء **فاتورة بيع**:
  - اختيار الطلب المراد عمل فاتورة له.
  - استدعاء كل معلومات الطلب تلقائياً.
  - تحديد طريقة الدفع.
  - تكون حالة الفاتورة: **قيد الانتظار** حتى يتم عمل مستند دفع.

- **سندات الدفع (للبيع)**:
  - تحديد الفاتورة المراد دفعها.
  - إمكانية الدفع **كامل** أو **بالتقسيط**.
  - عند الدفع الجزئي: حالة الفاتورة → **تم الدفع جزئياً**.
  - عند سداد المبلغ بالكامل (دفعة واحدة أو على دفعات): حالة الفاتورة → **تم الدفع**.

---

### 2. عمليات الشراء

- عند إنشاء **طلب شراء**:
  - تحديد المورد.
  - اختيار المنتجات.
  - تحديد المخزن لكل منتج.
  - يجب أن تكون حالة الطلب **مؤكدة** لكي يمكن عمل فاتورة له.

- عند إنشاء **فاتورة شراء**:
  - اختيار طلب الشراء المراد إصدار فاتورة له.
  - يتم فلترة الطلبات والفواتير الخاصة بالمورد المحدد.
  - عند الموافقة على الفاتورة:
    - يتم إضافة المنتجات إلى المخازن المحددة.
    - يتم إضافة إجمالي الفاتورة إلى حساب المورد (المبلغ المتبقي له).

- **سندات الدفع (للموردين)**:
  - تحديد الفاتورة المراد دفعها (جزئياً أو كلياً).
  - يتم الخصم من الحساب المتبقي للمورد.
  - حالة الفاتورة تتحول إلى:
    - **تم الدفع جزئياً** أو
    - **تم الدفع** حسب المبلغ المدفوع.
  - النظام يجمع مبالغ كل سندات الدفع المرتبطة بالفاتورة ويحدّث حساب المورد تلقائياً.

---

## 📊 رابعاً: التقارير

يتضمن النظام مجموعة من التقارير، مثل:

- تقرير **المخزون** لكل منتج في كل المخازن (يعتمد على مستند المخزون).
- تقرير **عمليات البيع** التي تمت في فترة زمنية محددة مع المبلغ الإجمالي.
- تقرير **عدد طلبات البيع لكل عميل** مع إجمالي المبالغ.
- **رسم بياني** يوضح الكميات المباعة وسعرها لكل يوم لمدة أسبوع، يتم تحديثه عند تأكيد فواتير البيع.
- تقارير أخرى تساعد في تحليل الأداء التجاري والإداري.

---

## 🎁 خامساً: العروض (الخصومات)

- قسم خاص لإدارة العروض (Promotions).
- عند إضافة عرض:
  - تحديد المنتج الخاص بالعرض.
  - تحديد نسبة التخفيض.
  - تحديد تاريخ انتهاء العرض.
- عند تفعيل العرض:
  - يتم حساب التخفيض تلقائياً في عمليات البيع.
- عند الوصول إلى **تاريخ انتهاء العرض**:
  - يتم إلغاء تفعيل العرض تلقائياً الساعة 12 ليلاً (بداية اليوم التالي).
  - لإعادة تفعيل العرض:
    - تحديد تاريخ انتهاء جديد.
    - تفعيل زر تفعيل العرض مرة أخرى.

---

## 🧩 ملاحظات عامة

- تم شرح أهم العمليات داخل النظام، وينصح بتجربة كل العمليات عملياً لفهم التدفق بشكل كامل.
- يمكن **التعديل على النظام حسب الاحتياج** لكل منشأة.
- لا يزال العمل مستمراً على:
  - تطوير النظام أكثر.
  - حل أي مشاكل بسيطة قد تظهر.
  - توسيعه ليشمل المزيد من العمليات الإدارية والتجارية.

---

شكراً لاستخدامك هذا النظام، وأي ملاحظات أو اقتراحات تطوير مرحب بها دائمًا.
